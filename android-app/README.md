# Hello World Android App

Minimal Android app written in Java using only Android SDK APIs.

Open this `android-app` directory in Android Studio and run the `app` configuration.

With Gradle and the Android SDK installed, build from this directory with:

```sh
gradle assembleDebug
```